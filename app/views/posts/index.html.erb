<div class="container">
	<% @posts.each do |post| %>
	<h2><%= post.title %></h2>
	<br>
	<div><%= truncate(post.body, :length => 34) %></div>
	<br>
	<%= link_to "Read Post", post , :class => "btn btn-info"%>
	<hr>
	<% end %>
</div>
